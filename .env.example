# MongoDB 数据库连接
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/xxb?retryWrites=true&w=majority

# NextAuth.js 认证配置
NEXTAUTH_SECRET=your-secret-key-here-minimum-32-characters
NEXTAUTH_URL=https://your-domain.vercel.app

# 腾讯云 COS 存储配置（可选）
TENCENT_SECRET_ID=your-tencent-secret-id
TENCENT_SECRET_KEY=your-tencent-secret-key
COS_BUCKET=xxb
COS_REGION=ap-guangzhou

# 部署说明：
# 1. 复制此文件为 .env.local 用于本地开发
# 2. 在 Vercel 项目设置中配置这些环境变量用于生产部署
# 3. NEXTAUTH_SECRET 可以通过 openssl rand -base64 32 生成
# 4. NEXTAUTH_URL 在生产环境中应该是你的实际域名
